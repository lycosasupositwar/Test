services:
  frontend:
    build:
      context: ./frontend
      args:
        - REACT_APP_API_URL=/api
    ports:
      - "8080:80"

  backend:
    build: ./backend
    volumes:
      - ./uploads:/app/uploads
      - db-data:/data
    environment:
      - DATABASE_URL=sqlite:////data/metallobox.db

volumes:
  db-data:
